{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "dotenv -- turbo dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "check": "turbo run format",
    "build:token": "turbo run build:token",
    "storybook": "turbo run storybook",
    "test:e2e": "turbo run test:e2e"
  },
  "devDependencies": {
    "@biomejs/biome": "1.7.2",
    "dotenv-cli": "latest",
    "lefthook": "^1.5.7",
    "turbo": "latest"
  },
  "workspaces": [
    "./apps/*",
    "./packages/*"
  ],
  "dependencies": {
    "build": "^0.1.4"
  }
}
