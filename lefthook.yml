pre-commit:
  parallel: true
  commands:
    build-token:
      glob: "packages/design-token/*"
      run: bun run build:token
    check:
      glob: "*.{js,ts,jsx,tsx,json}"
      run: bun run check
    test/e2e:
      run: bun run test:e2e
